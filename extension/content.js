// Prototype hook for future in-page annotation UI.
// The production version should render a small annotation popover when text is selected,
// then POST the annotation to the authenticated Next.js API.
